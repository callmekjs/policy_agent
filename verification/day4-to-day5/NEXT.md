# 내일 이어서 할 일

작업을 멈춘 시점: 커밋 `6247344` (원격에 올림, 작업 폴더 깨끗함)

## 지금 상태

누적 4일차 **코드는 끝났고, 재검토를 기다리는 중**이다.

1차 검토가 `BLOCKED`를 냈다. 초안 검사를 금지 목록으로 만들어 공격 47종 중
39종이 통과했다. 허용 목록으로 뒤집어 다시 만들었고, 공격 40종이 모두 막히는
것을 확인했다. **그 확인은 내가 한 것이라 아직 독립 검토를 받지 않았다.**

## 첫 번째로 할 일 — 4일차 재검토

`verification/day4-pass-bar.md`의 F1~J3만 판정 기준으로 쓰는 새 문맥 검토를
돌린다. 1차 검토가 뚫었던 39종을 **그대로 다시** 시도하게 하고, 더해서:

- 허용 목록의 빈틈: `draft_vocabulary.py`의 낱말로만 조립해 만들 수 있는
  거짓 문장이 있는가
- 조사·어미 떼기(`_strip_suffix`)를 이용한 우회
- 수 읽기(`numeral_reader.py`)가 놓치는 표기법
- 한 글자 낱말은 검사하지 않는 규칙(`len(stem) < 2`)을 이용한 우회

## 재검토가 PASS면 — 누적 5일차 (사람이 확인하고 수정)

만들 것은 넷이다.

1. 사실 확인 체크리스트 — 사람이 사실 하나씩 맞다/틀리다를 표시
2. `RevisionAgent` — 사람이 고쳐 달라고 한 것을 반영
3. 보호 사실·버전 보존 — 고친 뒤에도 중요한 값이 사라지지 않게
4. 답변 처리 API — 지금은 확인에 "예"를 누르면 새 작업이 만들어진다

## 재검토가 또 BLOCKED면

번호(F1~J3)를 확인하고 그 자리만 고친다. 번호를 댈 수 없는 지적은
`남은 일`로 내린다. 합격선을 바꾸는 것은 주석님만 한다.

## 5·6일차로 미뤄 둔 것

`verification/day4-to-day5/day4-self-measure.md`의 `남은 일` 목록과 1차 검토가
남긴 목록을 함께 본다. 특히:

- 화면을 새로 고치면 진행 중인 작업을 잃는다 (5일차)
- 적용례·경과조치가 초안에서 빠지면 차단하는 규칙이 없다 (5일차)
- 조문 집합 불일치 때 형식 재시도 1회가 없다 (5일차)
- 가짜 AI를 어떻게 할지 (6일차, 주석님이 전체 점검 때 결정)
