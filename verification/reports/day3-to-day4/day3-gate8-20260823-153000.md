# 개발 3일차 → 4일차 관문 검증 보고서 (여덟 번째)

- **run ID**: `day3-gate8-20260823-153000`
- **gate ID**: `DEMO-DAY3-GATE`
- **README 버전**: 0.23.0-draft
- **검사 대상 commit**: `55d2444` (fix: 버린 사실을 사람에게 보여 주고 범위 초과를 쉬운 안내로 바꾼다)
- **직전 보고서**: `day3-gate7-20260823-140500` (`BLOCKED`). 코드가 바뀌었으므로 `STALE`이며 이 보고서가 대체한다.
- **소스 snapshot**: git 추적 파일 121개(보고서·tmp 제외), 통합 해시
  `c59cbcecc34fc64013c1b75ff0eeaec284b41a5c23d4bb3aa870d35b9ce722e3`
- **판정**: **BLOCKED** (1급 2건, 2급 1건)
- 외부 AI 호출 0회 · 비용 0달러 · `.env` 열람 0회 · 제품 소스 수정 0건 · git commit/push 0건

---

## 1. 한눈에

| 검사 | 제목 | 결과 |
|---|---|---|
| `D3G-01` | **Gate 안전성** — 손으로 만든 raw 결과 직접 주입 | **FAIL** (B12·B13, 둘 다 1급) |
| `D3G-02` | B9 확인 — 버린 사실을 사람에게 보여 주는가 | PASS |
| `D3G-03` | B10 확인 — 범위 초과가 `NEEDS_INPUT`이고 안내가 사실인가 | PASS |
| `D3G-04` | B11 확인 — 다섯 목록의 자료 대조 / 고정 형식 | PASS |
| `D3G-05` | 진짜 충돌 검출 (`SYN-RISK-001` 3종, HTTP) | PASS |
| `D3G-06` | 2일차 회귀 (`verify-day1.ps1` 7개) | PASS |
| `D3G-07` | 새 시험이 형식적이지 않은지 (변이 시험) | **FAIL** (B14, 2급) |

### 담당자가 고쳤다고 한 세 가지는 실제로 고쳐졌다

- **B9** — 한 자료에 같은 줄이 두 번 있어 사실을 버릴 때, 이제 **값·자료명·행·원문을 함께** 보여 주고
  `NEEDS_INPUT`에서 멈춘다. 초안으로 넘어가지 않는다.
- **B10** — 지원 범위 초과가 `NEEDS_INPUT` + `subject=UNSUPPORTED_SCOPE`가 됐다.
  **`AI 결과를 쓸 수 없습니다`와 `잠시 뒤 다시 시도해 주세요`가 둘 다 사라졌다.**
  `ACTUAL-BLOCK-002`가 정답표대로 멈춘다. 같은 값 접기와 역할 후보 상한도 실제로 고쳐졌다.
- **B11** — 조문 비교가 현행·최종 짝을 각각 대조한다. 다른 자료의 근거를 빌리지 못한다.

### 그런데 같은 결함이 **고치지 않은 경로 두 곳**에 그대로 남아 있다

이번 관문은 지시대로 **가짜 추출기를 통째로 건너뛰고** `FakeModelGateway.set_response`로
손수 만든 raw 결과를 Gate와 Orchestrator에 직접 넣었다. 그 결과 두 가지가 나왔다.

- **B12** — `AMBIGUOUS`·`UNKNOWN_SOURCE`는 사람에게 보여 주는데,
  **`UNKNOWN_EVIDENCE`·`NOT_FOUND`는 보여 주지 않는다.** B9와 완전히 같은 모양이 남았다.
- **B13** — **값이 목록이면 Run 전체가 죽는다.** 고정 형식도 Pydantic도 목록을 허용하는데
  Gate의 한 줄이 처리하지 않는다.

---

## 2. 차단 3건

### B12 (🔴 1급) — 근거를 원문에서 못 찾으면 값도 충돌도 사라지고, 멈추지도 않는다

`backend/app/harness/orchestrator.py:82-86`

```python
blocking = [
    p
    for p in evidence.problems
    if p.kind in ("AMBIGUOUS", "UNKNOWN_SOURCE") and p.value
]
```

`EvidenceProblem.kind`는 네 가지다 — `NOT_FOUND`, `AMBIGUOUS`, `UNKNOWN_SOURCE`,
`UNKNOWN_EVIDENCE`. **이 목록에 두 개만 들어 있다.**

`_check_fact()`(`evidence_gate.py:295`)는 근거를 못 찾은 사실에 `UNKNOWN_EVIDENCE`를 붙이고
`value`까지 채워 둔다. 그런데 그 값이 위 필터를 통과하지 못해 **화면에 나가지 못한다.**

**손으로 만든 24조합 기계 훑기** (`근거 4형태 × 종류 3 × 자료 주장 2`)

불변식: *raw로 들어간 모든 사실 값은 원장에 남거나, 그 값을 이름으로 담은 기록에 남아야 한다.*

| 근거 모양 | 결과 | 충돌 | 사람에게 알림 | 멈춤 | 판정 |
|---|---|---:|---:|---|---|
| 정상 | 원장 또는 값과 함께 버림 | 1 / 0 | 0 / 1 | 예 | OK |
| 반복(같은 줄 2회) | 값과 함께 버림 | 0 | 1 | 예 | OK |
| **근거 문구가 원문에 없음** | **버림** | **0** | **0** | **아니오** | **값 소멸** |
| **없는 자료를 가리킴** | **버림** | **0** | **0** | **아니오** | **값 소멸** |

**24조합 중 12조합에서 값이 사라진다.**

**HTTP/Orchestrator 결정적 대조** — 의안정보(2207285)와 표결 결과(2209999)를 함께 넣고,
의안정보 쪽 근거 문구만 원문에 없는 것으로 바꿨다.

| 입력 | 상태 | 사실 | BLOCKING | 응답에 `2207285` |
|---|---|---:|---:|---|
| 근거가 원문에 있음 | `NEEDS_INPUT` | 2 | **1** `FACT_CONFLICT` | 있음 |
| 근거가 원문에 없음 | **`FAILED`/`DAY3_SCOPE_LIMIT`** | 1 | **0** | **없음(0건)** |

두 번째 실행의 화면은 이렇게 끝난다.

```
자료에서 사실 1건을 정리했습니다. 초안을 쓰는 단계는 아직 만들지 않았습니다.

근거를 찾지 못해 쓰지 않은 것 2건
  EV-01: 제시한 근거 문구가 자료 원문에 없습니다.
  F-01: 근거를 확인하지 못했습니다.
```

**값도, 자료명도, 행도, 인용도 없다.** 비전공자는 `F-01`이 무엇이었는지 알 수 없고,
두 자료가 의안번호에서 어긋났다는 사실 자체를 볼 방법이 없다.
그리고 **`DAY3_SCOPE_LIMIT`은 3일차의 정상 종료 지점이다.** 4일차에 초안 Agent가 붙는 바로 그 자리다.

**왜 이 경로가 중요한가.** `AMBIGUOUS`(같은 줄 반복)는 사용자가 이상한 자료를 넣어야 생긴다.
그런데 **`NOT_FOUND`는 AI가 원문에 없는 문장을 지어냈을 때 생긴다.** 근거 대조 Gate가
존재하는 이유 그 자체다. 누적 6일차에 진짜 AI가 붙으면 가장 자주 밟는 길이 된다.
지금은 Gate가 잡아내고도 **아무에게도 말하지 않는다.**

`_check_references()`도 이것을 막지 못한다. 근거 항목은 존재하고 **문구만 틀렸기 때문**이다.

**고칠 자리는 한 곳이다.** 위 튜플에 `"UNKNOWN_EVIDENCE"`를 넣으면 값·자료명·행이 이미
`EvidenceProblem`에 들어 있으므로 그대로 화면에 나간다. `NOT_FOUND`는 근거 항목 단위라
값이 없으므로, 그것을 참조하던 사실이 `UNKNOWN_EVIDENCE`로 잡히는 것으로 충분하다.

### B13 (🔴 1급) — 값이 목록이면 Run 전체가 죽고 정상 값·진짜 충돌까지 사라진다

`backend/app/gates/evidence_gate.py:335-340`

```python
def _normalize_value(fact: RawFact) -> str:
    return " ".join(fact.value.split())      # value가 목록이면 AttributeError
```

세 곳이 모두 목록을 허용한다.

- `test_sets/fact_extraction_result.schema.json`의 `fact_value`는
  `array (minItems 1, maxItems 8)`을 허용한다.
- `RawFact.value`는 `ShortText | list[ShortText]`이며 설명에 *“목록은 부칙 ID 묶음처럼
  여러 항목을 가리킬 때”*라고 적혀 있다.
- 바로 위 `_check_fact()`(`:297`)는 `", ".join(fact.value)`로 목록을 **일부러 처리한다.**

**이 한 줄만 처리하지 않는다.** 그래서 계약 안쪽의 정상 입력이 Gate를 죽인다.

**HTTP/Orchestrator 결과** — 정상 사실 2건(2207285 vs 2209999, 진짜 충돌 1건)에
`SUPPLEMENTARY_EFFECTIVE_DATES: ["2026-01-01", "2026-07-01"]` 하나를 더했다.

| | 목록 값 없음 | 목록 값 1건 추가 |
|---|---|---|
| 상태 | `NEEDS_INPUT` | **`FAILED`** |
| `failure_code` | — | **`AttributeError`** |
| 사실 | 2 | **0** |
| BLOCKING | 1 `FACT_CONFLICT` | **0** |
| `rejected_evidence` | 0 | **0** |
| 화면 문구 | 두 값과 자료명 | **`처리 중 문제가 생겼습니다.`** |
| 다음 할 일 | 어느 값이 맞는지 확인 | **`잠시 뒤 새 작업으로 다시 시도해 주세요.`** |

**값도 충돌도 어디에도 남지 않는다.** `rejected_evidence` 0건, `issues` 0건이다.
게다가

- `failure_code`가 파이썬 예외 이름(`AttributeError`)이라 사용자에게 아무 뜻이 없다.
- **`잠시 뒤 다시 시도해 주세요`는 틀린 처방이다.** 결정형이라 몇 번을 해도 같다.
  담당자가 B10에서 없앤 바로 그 거짓말이 다른 경로에 그대로 남아 있다.

`HIGH_RISK_FACT_KINDS`에 `SUPPLEMENTARY_EFFECTIVE_DATES`가 들어 있다. 즉 **목록 값은
가정이 아니라 설계가 예정한 모양**이며, 부칙에 시행일이 둘 이상인 자료
(`SYN-RISK-003`이 정확히 그렇다)에서 진짜 AI가 자연스럽게 만들 값이다.

### B14 (🟠 2급) — 이번에 고쳤다는 B9·B11에 시험이 하나도 없다

`git diff ca233f7 55d2444 -- tests/`는 **새 시험 0개**다. 시험 파일 변경은 기존
`test_상한을_넘으면_잘라내지_않고_멈춘다` 하나를 B10 동작에 맞춘 것뿐이다.

**변이 시험 10종** (scratchpad 사본에서만, 복원 후 baseline 실패 0건)

| 변이 | 무엇을 되돌리나 | 죽은 시험 |
|---|---|---:|
| **Y1** | `_rejected_fact_issues`의 blocking을 `[]`로 (**B9 전부 무효화**) | **0** |
| **Y2** | `describe()`가 내부 ID만 돌려줌 | **0** |
| **Y3** | 버린 사실에 자료명·행·원문을 안 담음 | **0** |
| **Y4** | `_keep_comparisons`를 통째로 통과 (**B11 전부 무효화**) | **0** |
| **Y5** | 조문 비교의 자료 대조만 끔 | **0** |
| **Y6** | 조문 비교의 반복 근거만 통과 | **0** |
| Y7 | 범위 초과를 다시 기술 오류로 | 1 |
| **Y8** | 같은 값 접기 제거 | **0** |
| **Y9** | 역할 후보 상한을 전체 누계로 되돌림 | **0** |
| Y10 | 사실의 자료 대조를 끔 (기준선) | 1 |

**10종 중 8종이 시험을 하나도 죽이지 못했다.** `Y1`·`Y4`는 전체 suite로도 다시 확인했고
baseline과 **완전히 같은 결과**가 나왔다.

즉 **이번 수정 셋 중 둘은 되돌려도 시험 106개가 전부 통과한다.** 회귀 검사기가 결함 있는
코드를 `PASS`로 보고한다. 다음 commit이 조용히 되돌려도 아무도 모른다.

직전 관문은 *“세 시험 모두 형식적이지 않다”*고 적었다. 그 세 시험은 지금도 유효하다
(`Y7`·`Y10`·`X` 계열). 문제는 **이번에 고친 것에 시험을 붙이지 않은 것**이다.

---

## 3. Gate 안전성 검사 (`D3G-01`) — 이번 관문의 주된 도구

가짜 추출기를 **전혀 거치지 않고** 세 층에서 손수 만든 raw 결과를 넣었다.

1. `build_fact_ledger` → `check_conflicts` 직접 호출 (19가지)
2. `FakeModelGateway.set_response`로 추출기를 통째로 대체한 Orchestrator 전체 경로 (14가지)
3. 조합 기계 훑기 (24조합)

| 손으로 만든 입력 | Gate가 한 일 | 판정 |
|---|---|---|
| **근거 없음** — 문구가 원문에 없음 | 버림. **값 안 보임, 충돌 소멸, 안 멈춤** | **B12 1급** |
| **근거 없음** — 없는 자료를 가리킴 | 위와 같음 | **B12 1급** |
| **근거 반복** — 한 자료에 같은 줄 2회 (고위험) | 버림 + 값·자료명·행·원문과 함께 `NEEDS_INPUT` | OK |
| 근거 반복 (저위험 종류) | 원장에 남음 (`validation.yaml` 규칙대로) | OK |
| **자료 불일치** — 다른 자료의 근거를 빌림 | 버림 + 값과 함께 `NEEDS_INPUT` | OK |
| **값 목록형** — `["2026-01-01","2026-07-01"]` | **AttributeError. Run 전체 소멸** | **B13 1급** |
| 값 목록형 + 정상 충돌 동반 | **정상 값과 진짜 충돌까지 함께 소멸** | **B13 1급** |
| **같은 종류 여러 값** (자료 간) | `FACT_CONFLICT` 1건, 두 값·자료명·행 표시 | OK |
| 같은 종류 여러 값 (**한 자료 안**) | `FACT_CONFLICT` 1건. 한 자료 안 모순도 잡는다 | OK |
| 같은 종류 같은 값 (앞뒤 공백만 다름) | 충돌 아님 (정규화가 반올림·자릿수를 바꾸지 않음) | OK |
| **30건 경계** — 29 / 30 | 통과. 29건·30건 그대로 원장에 | OK |
| **30건 경계** — 31 | 정지. 부분 결과 0건, 초안 0건 | OK (단 N9) |
| **빈 결과** | `NEEDS_INPUT` + `VERIFIED_FACT` 쉬운 질문 | OK |
| **`FACT_SCOPE_TOO_LARGE`** | `NEEDS_INPUT` + `UNSUPPORTED_SCOPE`, 안내가 사실 | OK |
| 한 근거에 사실 둘 (근거 공유) | 둘 다 원장에 남음 | OK |
| 값이 숫자 / 참·거짓 | `AGENT_SCHEMA_INVALID`로 안전 정지 (N1) | 계약 불일치 |
| 역할 미확정 + 버린 사실 동시 | 역할 질문만 보이고 버린 값은 가려짐 (N8) | 3급 |

**모든 Run에서 `draft_version=0`, `actual_model_calls=0`, `estimated_cost_usd=0`.**

---

## 4. B9·B10·B11 확인 (`D3G-02`·`D3G-03`·`D3G-04`)

### B9 (`D3G-02`) — PASS

직전 관문의 재현 입력(신구조문대비표 모양)을 그대로 넣었다.

```
다음 값은 근거를 한 곳으로 특정하지 못해 쓰지 않았습니다.
· 제12조 — 신구조문대비표 2행: “제12조를 고친다.” · 근거 문구가 자료에서 2군데 나와
  어디를 가리키는지 알 수 없습니다.

그 부분이 한 번만 나오는 공식 자료를 넣거나, 해당 대목만 잘라 넣어 주세요.
```

`NEEDS_INPUT`, 초안 0건. `RunStatusScreen.tsx:111-129`가 이 문구를 화면에 그대로 그린다.
사람이 실제로 읽을 수 있다.

### B10 (`D3G-03`) — PASS

**`ACTUAL-BLOCK-002` BASELINE (자료 6개, HTTP 끝까지)**

| 항목 | 결과 |
|---|---|
| 상태 | **`NEEDS_INPUT`** |
| Issue | `subject = UNSUPPORTED_SCOPE` |
| 안내 | `이 자료는 지금 버전이 한 번에 처리할 수 있는 범위를 넘습니다. …` |
| 다음 할 일 | `이번 보도자료에 꼭 필요한 공식 자료만 남겨 새 작업으로 다시 넣어 주세요.` |
| 초안 / 호출 / 비용 | 0건 / 0회 / 0달러 |

`oracle.md`가 요구한 *“지원 범위를 넘는다고 쉽게 설명하고 `NEEDS_INPUT`에서 새 작업을 안내”*가
성립한다. 금지 문구(`원안가결`)는 초안이 0건이라 나올 수 없다.
**AI를 탓하지 않고, 통하지 않는 방법을 권하지도 않는다.**

**같은 값 접기**도 값을 버리지 않고 겹침만 접는다.

| 고정 자료 | 7차 | 8차 |
|---|---:|---:|
| ACTUAL-PASS-001 | 13 | 10 |
| ACTUAL-BLOCK-002 | 0 (`FACT_SCOPE_TOO_LARGE` 42건) | 0 (`UNSUPPORTED_SCOPE` 31건) |
| SYN-RISK-001 | 16 | 15 |
| SYN-RISK-003 | 24 | 21 |

역할 후보 상한도 자료별로 고쳐졌다. 내용이 같은 `잘 모르겠음` 자료 두 개에서
`SRC-01: 3건, SRC-02: 3건` (이전에는 3+1건).

### B11 (`D3G-04`) — PASS

**손으로 만든 raw로 다섯 목록을 각각 확인했다.**

| 항목 | 다른 자료 근거를 빌림 | 반복 근거 | 근거 없음 | 정상 |
|---|---|---|---|---|
| 입법 사건 | 버려짐 `UNKNOWN_SOURCE` | 버려짐 `AMBIGUOUS` | 버려짐 | — |
| 부칙 | 버려짐 `UNKNOWN_SOURCE` | 버려짐 `AMBIGUOUS` | 버려짐 | — |
| 의안 | 버려짐 `UNKNOWN_SOURCE` | 버려짐 `AMBIGUOUS` | 버려짐 `UNKNOWN_EVIDENCE` | — |
| 관계 | 버려짐 `UNKNOWN_SOURCE` | 버려짐 `AMBIGUOUS` | 버려짐 | — |
| **조문 비교** | **버려짐 `UNKNOWN_SOURCE`** | **버려짐 `AMBIGUOUS`** | **버려짐 `UNKNOWN_EVIDENCE`** | **원장에 남음** |

**직전 관문의 B11이 실제로 닫혔다.** `_keep_comparisons`가 `current`·`final` 짝을 각각
대조한다.

**고정 형식** — `jsonschema 4.26.0`으로 가짜 추출기 출력 5가지를 검사해 **위반 0건**.
`FACT_SCOPE_TOO_LARGE` 응답도 아홉 배열의 `maxItems: 0`을 지킨다.

---

## 5. 진짜 충돌은 여전히 잡는다 (`D3G-05`)

`SYN-RISK-001` 자료 5개 전부, mutation의 `target_id = SR1-S05`를 지켜 HTTP로 실행했다.

| 변이 | 결과 | 화면 문구 |
|---|---|---|
| `COUNT_CONFLICT` | `NEEDS_INPUT` · `FACT_CONFLICT`/`vote_yes_count` | `201 — 03_plenary_vote.md 8행` vs `202 — 05_independent_vote_notice.md 6행` |
| `DATE_CONFLICT` | `NEEDS_INPUT` · `FACT_CONFLICT`/`plenary_decided_on` | `2026. 8. 20` vs `2026. 8. 21` |
| `WEEKDAY_MISMATCH` | `NEEDS_INPUT` · `DATE_WEEKDAY_MISMATCH` | `2026. 8. 20은(는) 목요일인데 자료에는 금요일로 적혀 있습니다. 자동으로 고치지 않았습니다.` |

3종 모두 사실 15건, `draft_version = 0`, `actual_model_calls = 0`, `estimated_cost_usd = 0`.
BASELINE(변조 없음)은 차단 0건으로 지나간다. **거짓 충돌 증가 0건.**

`ACTUAL-PASS-001`(정상 자료)도 차단 0건, 사실 10건이 원문 근거와 연결된 채 통과한다.

---

## 6. 2일차 회귀 (`D3G-06`)

```
powershell -ExecutionPolicy Bypass -File C:\policy_agent\scripts\verify-day1.ps1 -RunId day3gate8-regress
```

script 출력을 **직접 확인했다.**

- `D1G-01` 필수 파일·백엔드 모듈 로드 exit 0
- `D1G-05` 물리적 파일 5개, Contract parse exit 0
- `D1G-06` 백엔드 pytest / 프런트 typecheck / vitest / production build 전부 exit 0
- `D1G-02` `start-local.cmd`로 8765 health 200, `stop-local.cmd` 정상 종료
- `D1G-03`·`D1G-04` 네 가지 입력, 빈 입력 차단, 멱등 키 재사용, 쿠키 없는 요청 403, 경로 탈출 6종 차단
- `D1G-07` API 키 패턴 0건, `.env` 접근 0건, wildcard CORS 0건, 생명과학 잔재 0건
- **`=== 판정: PASS ===`**

백엔드 단위시험도 별도로 `106 passed`.

---

## 7. 미룰 것 (숨기지 않고 적어 둔다)

| ID | 급 | 내용 | 언제 |
|---|---|---|---|
| N1 | 🟡 | 고정 형식이 허용하는데 Pydantic이 거부하는 입력 **6건** — 값이 숫자·참/거짓, 값 450자(500/400), 근거 문구 800자(1000/600), id 100자(128/64), `procedure_stage`·`disposition` 자유문자(text/enum). 전부 안전하게 멈추지만 `잠시 뒤 다시 시도`라는 틀린 처방을 준다 | 누적 7일차 **형식 통일** |
| N2 | 🟡 | `ACTUAL-BLOCK-002`가 사실 31건으로 상한 30을 **1건 차이**로 넘겨 막힌다. manifest의 진짜 차단 사유(`UNSUPPORTED_ORIGIN_BILL_COUNT=11`, `UNSUPPORTED_CHANGED_PROVISION_COUNT=6`, `gate=PROFILE_SCOPE`)는 아직 없다 | 누적 9일차 **법률 단계와 조문** |
| N3 | 🟡 | `MAX_SOURCES=6`을 화면에 알리면서 촘촘한 표결 자료는 4개부터 막힌다(32/40/48건). 다만 안내가 정직해졌고 실제 고정 자료는 자료 6개까지 통과한다 | 누적 7일차 |
| N4 | 🟡 | `SYN-RISK-003` 정상본이 거짓 충돌 3건으로 막힌다(원안 vs 대안, 대안반영폐기 vs 원안가결, 제8조·제12조·제1조). **고정 형식이 Agent에게 `kind`와 `value`만 허용해 서로 다른 대상을 구분할 칸이 없다** | 추출 품질은 누적 6일차, 구조는 누적 9일차 |
| N5 | 🟡 | 화면 `rejected_evidence` 절의 고정 안내문이 반복 근거 경우에 사실이 아니다 (`RunStatusScreen.tsx:97-99`) | 누적 7일차 |
| N6 | 🟡 | `run.rejected_evidence`가 아직 `내부ID: 설명`이다. `describe()`를 쓰지 않는다 | 누적 11일차 |
| N7 | 🟡 | 사건·부칙·의안·관계의 problem에 `value`·`source_name`·`raw_line`이 비어 있다. 조문 비교만 채운다 | 누적 11일차 |
| N8 | 🟡 | 역할 확인 Issue가 있으면 버린 사실 안내가 가려진다. `NEEDS_INPUT`에서 멈추므로 초안으로 새지는 않는다 | 누적 11일차 |
| N9 | 🟡 | 진짜 AI가 사실 31건을 돌려주면 `FACT_SCOPE_TOO_LARGE`가 아니라 Pydantic 상한으로 `AGENT_SCHEMA_INVALID`가 된다. B10의 쉬운 안내가 붙지 않는다 | 누적 6일차 전 |

---

## 8. 마지막 질문에 대한 답

> **3일차 종료 조건 “정상 자료는 사실과 원문 근거가 연결되고, 부족·충돌 자료는 초안 없이
> 쉬운 질문에서 멈춤”이 성립합니까?**

### 8.1 답: **앞쪽 절반은 성립한다. 뒤쪽 절반이 아직 성립하지 않는다.**

| 종료 조건 | 상태 |
|---|---|
| 정상 자료는 사실과 원문 근거가 연결된다 | **성립한다.** `ACTUAL-PASS-001` 10건, `SYN-RISK-001` 15건이 값·자료명·행·인용과 함께 원장에 남는다. 직전 관문의 “자료 4개부터 전체가 막힘”은 실제 고정 자료에서는 사라졌다(자료 6개, 21건까지 통과) |
| 부족·충돌 자료는 **초안 없이** 멈춘다 | **성립한다.** 이번 검증의 모든 Run에서 `draft_version=0`이다 |
| 부족·충돌 자료는 **쉬운 질문에서** 멈춘다 | **성립하지 않는다.** 근거를 원문에서 못 찾으면 질문이 아예 생기지 않고 정상 종료 지점까지 간다(B12). 값이 목록이면 질문 대신 `처리 중 문제가 생겼습니다`가 뜬다(B13) |

갖춰진 것도 분명하다. **일곱 번 반복됐던 회의 구분 축은 닫힌 채 유지되고**, 원문 보존·
근거 대조·상한 경계·다섯 목록의 자료 대조·범위 초과의 정직한 안내가 모두 갖춰졌다.
직전 관문이 지적한 B9·B10·B11 셋은 **전부 실제로 고쳐졌다.**

### 8.2 지금 고칠 것과 미룰 것

**지금(3일차 안에) 반드시 고칠 것 — B12·B13 둘뿐이다.**

- **둘 다 규칙 조정이 아니다.** B12는 `orchestrator.py:85`의 튜플에 낱말 하나를 넣는 것이고,
  B13은 `evidence_gate.py:340`이 목록을 문자열로 만드는 것이다.
  **정규식은 한 글자도 건드리지 않는다.** 여섯 번 반복됐던 진자에 속하지 않는다.
- **둘 다 4일차를 직접 부순다.** 4일차는 초안 Agent가 **원장을 읽기 시작하는** 단계다.
  B12로 사라진 값과 충돌은 사용자도 초안 Agent도 존재를 모른 채 그대로 초안에 들어간다.
  B13은 부칙 시행일이 둘 이상인 자료(`SYN-RISK-003`이 바로 그렇다)에서 4일차 입력이
  아예 도달하지 못하게 한다.
- **B12는 누적 6일차 전에는 반드시 닫아야 한다.** 진짜 AI가 원문에 없는 문장을 지어내는
  것은 근거 대조 Gate가 존재하는 이유 그 자체다. 지금은 잡아내고 말하지 않는다.

**함께 고칠 것 — B14 (시험).**

- B9·B11 두 수정에 시험이 없어 **되돌려도 106개가 전부 통과한다.** 여덟 번을 고쳐 온
  이 파일들에서 시험 없는 수정은 아홉 번째를 부른다.
- 최소한 네 개면 된다. ① 버린 사실이 값·자료명·행과 함께 `issues`에 나온다,
  ② 조문 비교가 빌린 근거를 버린다, ③ 근거를 못 찾은 사실도 사람에게 보인다(B12),
  ④ 목록 값이 죽지 않는다(B13).

**미룰 것 — N1~N9.** 위 7절 표에 근거와 함께 적었다. README §3.7 기준으로
누적 6·7·9·11일차에 각각 자리가 있다. 가짜 추출기의 **추출 품질**(N4의 거짓 충돌,
어떤 표현을 놓치는지)은 전부 **누적 6일차**로 보낸다.

### 8.3 “여덟 번째도 반복인가” — 냉정한 판단

**아니다.** 직전 관문이 지적한 세 건은 **전부 고쳐졌고**, 여섯 번 반복됐던 정규식 축은
이번에도 손대지 않았다. 담당자는 조언을 지켰다.

남은 둘은 **새로 만든 결함이 아니라, 고친 결함과 같은 자리에 있던 형제**다.
B9를 고치면서 네 가지 문제 종류 중 둘만 목록에 넣었고, 값을 문자열로만 가정한 줄 하나를
지나쳤다. **둘 다 한 줄이다.** 이번에는 시험을 함께 붙이면 아홉 번째가 없다.

---

## 9. 4일차로 넘어가도 되는가

**아직 안 된다. 다만 남은 거리는 매우 짧다.**

4일차는 초안 Agent가 원장을 읽기 시작하는 단계다. 지금 상태로 넘어가면 다음이 그대로
초안에 들어간다.

- **B12로 사라진 값과 사라진 충돌** — 사용자도 초안 Agent도 존재를 모른다. AI가 지어낸
  근거는 조용히 버려지고, 남은 한쪽 값만 초안에 쓰인다.
- **B13으로 부칙 시행일이 여럿인 자료** — 4일차 입력에 도달하지 못한다.

**4일차 착수 전 필수 수리: B12(1급), B13(1급). 둘 다 한 줄이다.**
**함께 붙일 것: B14 — 이번 수정 넷에 시험 넷.**

---

## 10. 검사원이 지킨 것

- 제품 소스·테스트·Writing Contract·README·schema 수정 **0건**.
  변이는 scratchpad 사본에서만 했고, 검증을 마친 뒤 통합 해시를 다시 계산해 시작 시점과
  같음을 확인했다: git 추적 파일 121개, `git ls-files -s` 해시
  `997ab51167ba925a19584d99da8de258abe563f47dc85784d80a9017666cebca`.
  작업 트리에 늘어난 것은 이 보고서 2개와 `verify-day1.ps1`이 만든 1일차 보고서 2개뿐이다.
- 실패 기준 완화·테스트 삭제 0건 / 패키지 설치·lock 변경 0건
- 인터넷·OpenAI API 호출 **0회**, 비용 **0달러**
  (`/api/health` → `model_gateway=fake`, `external_api_calls=0`.
  이번 검증의 모든 Run이 `actual_model_calls=0`, `estimated_cost_usd=0`)
- `.env` 열람 0건 / git commit·push 0건
- 코드를 고친 뒤 스스로 PASS 처리 0건
- 띄운 서버·프로세스 정리 완료, 포트 8765·8799 비어 있음

### schema 미통과 (지시대로 고치지 않음)

이 보고서의 `.json`은 `verification/day-gate-result.schema.json`을 통과하지 못한다.
schema가 `gate_id`를 `const "DEMO-DAY1-GATE"`로, `check_id`를 `enum D1G-01~07`로 고정하고
있기 때문이다. 위반은 `gate_id` 1건 + `check_id` 7건 = **8건**뿐이고 나머지 필드는 모두
schema를 따른다. 3일차용 gate/check ID를 schema에 추가하는 것은 담당자 몫이다.
