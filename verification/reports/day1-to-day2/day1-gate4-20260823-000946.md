# 개발 1일차 → 2일차 검증 보고서 (4차, 독립 검사원)

- 관문: `DEMO-DAY1-GATE`
- run ID: `day1-gate4-20260823-000946`
- README 버전: 0.23.0-draft
- 시작: 2026-08-22T14:50:00Z / 종료: 2026-08-22T15:12:00Z
- 소스 파일 36개, 통합 해시 `42ebe12e57e16e06edc757464ed303f679f3d620e4696ea7f85ce8c9c808e4f2`
- 외부 AI 호출 0회, API 비용 0달러, `.env` 열기 0회(크기 378바이트만 확인)
- 검사원: 구현 담당자와 **다른 새 문맥** (`is_separate_from_implementer=true`)

## 판정: **PASS**

`DEMO-DAY1-GATE=PASS`. 필수 검사 7개 전부 PASS, 차단 문제 0건, 필수 증거 누락 0건.

| 검사 | 내용 | 결과 |
|---|---|---|
| `D1G-01` | 1일차 필수 파일과 실행 진입점 | **PASS** |
| `D1G-02` | 한 주소에서 시작·종료 (문서화된 실행법) | **PASS** |
| `D1G-03` | 사용자 입력 네 가지 | **PASS** |
| `D1G-04` | Harness와 가짜 AI | **PASS** |
| `D1G-05` | Writing Contract manifest와 설정 | **PASS** |
| `D1G-06` | 반복 가능한 코드 검사 | **PASS** |
| `D1G-07` | 비밀·네트워크·기록 | **PASS** |

## 앞선 세 차단 문제의 현재 상태

| 회차 | 차단 문제 | 현재 | 검사원이 직접 확인한 증거 |
|---|---|---|---|
| 1차 | 경로 탈출로 `.env` 유출 | **해결 유지** | raw socket 37종을 직접 보내 유출 0건. 200 응답 33건은 전부 첫 화면(`<div id="root">`), 404가 4건. 제품 코드는 3차 PASS 시점과 파일 해시가 완전히 같아 회귀 가능성 자체가 없음 |
| 2차 | `start-local.cmd`가 LF 줄바꿈으로 실행 실패 | **해결 유지** | start-local.cmd CRLF 79줄·LF 단독 0개, stop-local.cmd도 전부 CRLF, `.gitattributes`의 `*.cmd text eol=crlf` 유지. 5회 실행 모두 오류 메시지 0건 |
| 3차 | 검사 script가 실행기를 일찍 끊어 고아 서버가 남고 gate가 비결정적 | **해결** | 결정형 script 연속 5회 전부 PASS·exit 0(3차의 1/3 실패 재현 0건). 실행기 강제 중단 12종 + 고아 재현 3종, 합계 15/15가 `stop-local.cmd` **1회**로 정리. 정리 뒤 `start-local.cmd` 재실행이 health 200으로 복귀해 '막다른 길'도 해소 |

## 구현 담당자가 주장한 수정 5건 확인 결과

| # | 주장 | 확인 |
|---|---|---|
| 1 | `start-local.cmd`가 서버를 띄운 **직후** 번호를 기록 | **사실이고 실효가 있다** — 강제 중단 시점을 0.2~5초로 훑은 결과 pid 파일이 없는 창은 약 0.8~1.4초로 좁고, 그 구간에서 끊어도 stop-local.cmd 1회로 정리됨 |
| 2 | `stop-local.cmd`에 명령줄 기반 복구 경로 추가 | **사실이고 실효가 있다** — pid 파일이 없는 고아 서버를 3/3 모두 1회로 정리하고 `남아 있던 서버를 종료했습니다`를 출력 |
| 3 | `pause` 제거, 포트 사용 중 안내를 실행 가능한 행동으로 변경 | **사실** — 두 `.cmd` 전체에 `pause` 0건. 포트가 잡혔을 때 `stop-local.cmd 를 실행하면 남은 서버를 찾아 정리합니다`로 안내 |
| 4 | `verify-day1.ps1`이 실행기가 스스로 끝나기를 기다리고 번호 기록까지 확인 | **사실** — `$proc.WaitForExit(120000)` 뒤에 health를 보고, `LauncherExited`·`PidRecorded`를 각각 별도 FAIL 사유로 둠. 5회 모두 `실행기가 스스로 종료하고 서버 번호를 기록함` 증거 생성 |
| 5 | `.gitignore`에 `.local-server.pid` 추가 | **사실** — 파일 끝에 항목 존재, 서버 기동 중 `git status`가 깨끗함 |

추가로 "연속 3회 PASS, 남은 포트 0개"라는 주장도 **검사원이 5회로 늘려 재현**했다(5/5 PASS, 매회 8765·8799 listener 0개, python 프로세스 0개, pid 파일 없음).

---

### D1G-01 1일차 필수 파일과 실행 진입점 — PASS

- 결정형 script D1G-01 구간: 필수 파일 28개 모두 존재, 누락 0건 (5회 연속 동일)
- 백엔드 모듈 로드 exit 0 — `app.main`, `app.api.runs`, `app.harness.orchestrator` import 성공
- 검사원이 트리를 직접 확인: `backend/app` 파이썬 16개 + Writing Contract YAML 5개, `frontend/src` 8개, `tests` 2개
- React·FastAPI·Harness 뼈대·검증 도구 파일이 모두 실재
- 소스 snapshot 36개 파일의 SHA-256을 **검사원이 독립적으로 다시 계산**해 script 값과 per-file 완전 일치

실행한 명령:

- `powershell -ExecutionPolicy Bypass -File scripts\verify-day1.ps1` (D1G-01 구간) ×5 → exit 0
- `python -c "import app.main, app.api.runs, app.harness.orchestrator; print('modules ok')"` → exit 0
- 검사원 자체 snapshot 재계산 → `file_count=36`, `combined=42ebe12e…e4f2`, script 보고서와 일치

### D1G-02 한 주소에서 시작·종료 (문서화된 실행법) — PASS

- 줄바꿈 회귀 없음: `start-local.cmd` CRLF 79줄·LF 단독 0개
- **결정형 script 연속 5회 전부 PASS·exit 0.** 매회 실행기가 스스로 종료하고 번호를 기록했으며 `stop-local.cmd` 뒤 8765 listener 0개
- 실행기 stdout에 `준비됐습니다. 브라우저를 엽니다: http://127.0.0.1:8765`가 찍혀 브라우저 실행 줄까지 도달
- **실행기 강제 중단 12종** (0.2/0.5/0.8/1.1/1.5/2/3/5초 + 서버가 뜨는 즉시 끊기 4회) → 12/12 모두 `stop-local.cmd` 1회로 정리, ORPHAN-LEFT 0건
- **3차 차단 문제의 정확한 재현 3회** (같은 명령줄로 서버만 띄우고 pid 파일 없음) → 3/3 CLEAN, 직후 `start-local.cmd` 재실행이 exit 0·health 200·pid 기록으로 복귀
- 자신이 시작한 것만 종료: 8765를 쓰는 무관한 python(`http.server`)과 비-python(.NET `TcpListener`)은 건드리지 않고 `정리할 서버가 없습니다`로 끝남
- `/api/health` 200 (`assembly_member_partial_amendment_plenary_v1@1.0.0`), 첫 화면 200에 `<div id="root">` 포함

실행한 명령:

- `powershell -ExecutionPolicy Bypass -File scripts\verify-day1.ps1` ×5 (`day1-gate4-run1`~`run5`) → 모두 exit 0 / 판정 PASS
- 실행기 강제 중단 12종 후 `stop-local.cmd` 1회 → ORPHAN-LEFT 0건
- 고아 재현 3회 → 3/3 CLEAN + RESTART-OK
- 미끼 프로세스 대조 4종 → 무관한 프로세스 전부 생존
- `grep -i pause start-local.cmd stop-local.cmd` → 일치 0건

### D1G-03 사용자 입력 네 가지 — PASS

- 화면(`NewRunScreen.tsx`)에 `보도 목적`·`공식 자료`·`공개 범위`·`자료 확인 기준일` 라벨이 모두 있고 각각 필수 표시
- API 계약(`CreateRunRequest`)과 입력 Gate(`check_input`)에 네 입력이 모두 존재
- 빈 입력은 거짓 성공 없이 차단: `NEEDS_INPUT`, `draft_version=0`, 쉬운 안내 2건 (5회 동일)
- 잘못된 값도 코드로 차단: 공개 범위≠PUBLIC → `DISCLOSURE_NOT_ALLOWED`, 미래 기준일 → `BASIS_DATE`, 자료 없음 → `REQUIRED_SOURCE_MISSING`
- 요청 토큰 쿠키 없는 POST는 403, 외부 Origin POST는 403 `ORIGIN_NOT_ALLOWED` (ACAO 헤더 없음)

실행한 명령:

- `powershell -File scripts\verify-day1.ps1` (D1G-03 구간) ×5 → exit 0
- raw socket `POST /api/runs` with `Origin: https://evil.example.com` → 403 `ORIGIN_NOT_ALLOWED`

### D1G-04 Harness와 가짜 AI — PASS

- 새 Run ID 형식이 계약과 일치: `^RUN-[A-Z0-9]{12}$` (5회 모두 통과)
- 상태가 계약 안에 있음. `ALLOWED_TRANSITIONS`가 README §2.13 표와 일치하고 종료 상태에서 나가는 전이 0개
- 1일차 최소 전이 확인, 미구현 단계는 `FAILED/TECHNICAL/DAY1_SCOPE_LIMIT`로 정직하게 멈춤
- 가짜 `ModelGateway`만 사용 (`model_gateway=fake`, `external_api_calls=0`), 제품 코드에 실제 OpenAI 호출 경로 0건
- 입력 Gate 단계 AI 호출 0회, 검증 전 구간 외부 호출 0회·비용 0달러
- 같은 멱등 키는 기존 Run 재사용
- pytest 67개 통과 (검사원이 script와 별개로 재실행)

실행한 명령:

- `powershell -File scripts\verify-day1.ps1` (D1G-04 구간) ×5 → exit 0
- `python -m pytest ../tests -q` → exit 0 — 67 passed in 3.38s
- `grep -rn "from openai|import openai|api.openai.com" backend/app frontend/src` → 일치 0건

### D1G-05 Writing Contract manifest와 설정 — PASS

- 물리적 파일 5개 존재: `contract.yaml`, `profile.yaml`, `template.yaml`, `style.yaml`, `validation.yaml`
- 서버가 실제로 읽음: `load_writing_contract()` → `assembly_member_partial_amendment_plenary_v1@1.0.0`
- ID·버전·필수 필드 검사가 코드에 실재하고, 어긋나면 `ContractLoadError`로 서버가 시작하지 않음
- lifespan이 Contract 로드를 기동 조건으로 두므로 다섯 파일이 온전할 때만 `/api/health`가 뜸
- 응답에는 파일 이름만 나가고 내부 절대 경로가 새지 않음

실행한 명령:

- `powershell -File scripts\verify-day1.ps1` (D1G-05 구간) ×5 → exit 0
- `python -c "from app.harness.contract_loader import load_writing_contract as l; c=l(); print(c.full_id)"` → exit 0

### D1G-06 반복 가능한 코드 검사 — PASS

- 백엔드 Python 구문·모듈 로드 exit 0
- 백엔드 단위시험 exit 0 — 67 passed (검사원 직접 재실행)
- 프런트 typecheck exit 0 — 출력 없음
- 프런트 test exit 0 — vitest 6 passed
- 프런트 production build exit 0 — `dist/index.html` 0.45kB, css 3.17kB, js 206.87kB
- 로컬 API/UI smoke exit 0
- 필수 명령 누락·SKIP 0건, 5회 연속 동일

실행한 명령:

- `python -m pytest ../tests -q` → exit 0 — 67 passed
- `npx tsc --noEmit -p tsconfig.app.json` → exit 0
- `npm test --silent` → exit 0 — Tests 6 passed
- `npm run build --silent` → exit 0 — built in 485ms

### D1G-07 비밀·네트워크·기록 — PASS

- **경로 탈출 차단 (1차 회귀 확인)**: raw socket 37종 직접 시도 → 유출 0건. `..` 원시형, `%2e`/`%2f`/`%5c`, 이중 인코딩, overlong UTF-8, NULL 바이트, `::$DATA`, 끝의 점·공백, 드라이브 절대경로, `/assets` mount 경유, 세미콜론 경로 파라미터, absolute-form 요청줄 포함
- 제품 코드 무변경 확인: 3차 PASS snapshot과 대조해 `backend/app`·`frontend/src`·`tests` 해시 전부 동일, 바뀐 파일은 `verify-day1.ps1`·`start-local.cmd`·`stop-local.cmd` 3개뿐
- 빌드 산출물 확인이 차단 조건으로 존재하고, 이번 5회 모두 실제 `dist`를 대상으로 검사
- 실제 OpenAI 호출 0건·비용 0달러, `.env` 접근 0건(검사원도 열지 않고 크기 378바이트만 확인)
- API 키 패턴 0건 (소스·`frontend/dist` bundle·tests·scripts·신규 보고서 포함)
- wildcard CORS 0건, 생명과학 전용 잔재 0건, `dangerouslySetInnerHTML` 0건
- `Host: evil.example.com`으로 보낸 `GET /api/runs/{id}` → 403 `NON_LOCAL_HOST`
- 비밀 없는 최신 보고서 생성, 기존 보고서 덮어쓰기 0건, script 보고서 5개 전부 schema 검증 통과(오류 0건)

실행한 명령:

- raw socket 경로 탈출 37종 (검사원 자체 probe, 8799) → LEAKS=0
- raw socket `GET /api/runs/RUN-000000000000` with `Host: evil.example.com` → 403 `NON_LOCAL_HOST`
- `python` + jsonschema 4.26.0 로 `day1-gate4-run1~5` 검증 → schema_errors=0
- `grep -rInE 'sk-…|OPENAI_API_KEY' 보고서·dist` → 일치 0건
- 3차 보고서 snapshot과 파일별 해시 대조 → 바뀐 파일 3개뿐

## 차단 문제

없음 (0건).

## 경고 — 차단은 아니지만 남겨 두고, 갈 곳을 지정한다

| 경고 | 검사원 판단 | 미룰 곳 (README 근거) |
|---|---|---|
| `/api/health`·`/api/bootstrap`·`/api/contract`에 Host 검사 없음 | 차단 아님. 세 응답에 든 값은 앱 제목·Contract ID·모델 이름·입력 한도·자료 역할뿐이고 비밀값·사용자 원문·내부 경로 0건. 사용자 자료 경로와 모든 상태 변경은 이미 403 | **누적 10영업일 (안전 보강 4일차)** — §3.7 "내부·엠바고·민감정보, 자료 속 악성 지시, 다른 사이트 요청(Origin), 화면 스크립트 공격(XSS), 비밀값 노출을 차단" |
| `dist`가 없으면 pytest의 경로 탈출 시험 14종이 공허하게 통과 | 차단 아님. 관문 전체는 공허하지 않다 — D1G-06이 production build를 돌리고 D1G-07이 `dist/index.html` 부재를 FAIL로 처리하며, 이번 5회 모두 실제 `dist`에 대해 라이브 검사가 돌았다. 시험 자체에 `dist` 존재 단언 추가를 권고 | **누적 10영업일 (안전 보강 4일차)** |
| `server: uvicorn` 헤더 노출, CSP·`X-Content-Type-Options` 없음 | 차단 아님. D1G-07 필수 항목이 아니고 XSS·비밀값 노출 차단이 배정된 날이 따로 있다 | **누적 10영업일 (안전 보강 4일차)** |
| `GET /api/runs/{긴 문자열}`이 run_id를 되비춤 | 차단 아님. JSON이고 React가 escape | **누적 10영업일 (안전 보강 4일차)** |
| `stop-local.cmd`가 pid 파일의 번호가 이미 죽었을 때 복구 검색으로 내려가지 않음 | 차단 아님. 정상 사용 경로에서 재현되지 않고(15/15가 1회로 정리), 재현하려면 pid 파일을 손으로 바꿔야 한다. 이 경우에만 두 번 눌러야 정리됨 | **누적 15영업일 (안전 보강 9일차: 삭제와 종료 — 서버 재시작·안전 종료)** |
| 복구 경로가 명령줄 문자열로 대상을 고름 | 차단 아님. 무관한 프로그램(8765를 쓰는 python `http.server`, 비-python `TcpListener`, 다른 app/port의 uvicorn 문자열)은 하나도 건드리지 않았다. 손으로 띄운 **같은 프로그램의 서버**만 함께 꺼진다 | **누적 15영업일 (안전 보강 9일차)** |
| 멱등 기록의 2시간 별도 보관 없음 | 차단 아님. `day-gate-verifier.md`에 이미 미룬 일로 적혀 있음 | **누적 14영업일 (중복·재시도·비용)** |
| 실행 바로가기에서 브라우저 창이 실제로 뜨는지 | 차단 아님. stdout에 브라우저 실행 줄까지 도달함을 확인. 화면 확인은 사람 1회 | **1일차 종료 확인의 사람 1회** (`day-gate-verifier.md`에 명시) |

## 1일차 종료 조건 (README §3.7 누적 2일차 "하루 종료 확인")

| 종료 조건 | 결과 |
|---|---|
| 실행 바로가기에서 브라우저가 열림 | **조건부 충족** — `start-local.cmd`가 5회 모두 `준비됐습니다. 브라우저를 엽니다: http://127.0.0.1:8765`까지 도달. 창이 실제로 보였는지는 사람이 한 번 눈으로 확인 |
| 보도 목적·공식 자료·공개 범위·자료 기준일을 입력할 수 있음 | **충족** — 화면 라벨 4종·API 계약·입력 Gate 모두 확인, 잘못된 값은 거짓 성공 없이 차단 |
| Writing Contract manifest 1개 + 설정 4개를 서버가 읽음 | **충족** — 물리 파일 5개, `assembly_member_partial_amendment_plenary_v1@1.0.0` 로드 |
| 다른 새 문맥의 `DayGateVerifier`가 `D1G-01~07`을 모두 확인해 최신 `DEMO-DAY1-GATE=PASS` 보고서를 남김 | **충족** — 이 보고서 |

## 개발 2일차 진행 여부

**시작할 수 있다.** README §3.7 누적 3일차의 전제는 "현재 소스 snapshot과 일치하는 최신 `DEMO-DAY1-GATE=PASS`가 있을 때만 시작"이고, 이 보고서의 snapshot 해시 `42ebe12e…e4f2`는 검사 시점의 현재 트리와 정확히 같다.

다만 다음 두 가지를 함께 지켜야 한다.

1. 남은 **사람 1회 확인**(실행 바로가기를 눌러 브라우저 창이 뜨는지 눈으로 보기)을 1일차 종료 확인으로 마칠 것.
2. 이 보고서 뒤에 제품 코드·설정·테스트가 한 줄이라도 바뀌면 이 보고서는 즉시 `STALE`이며, 2일차를 시작하기 전에 다시 검증할 것.

## 검사원이 정리한 것

- 결정형 script 5회 실행이 남긴 서버 — 매회 script가 스스로 정리, 검사원의 추가 종료 0건
- 강제 중단 시험 12종·고아 재현 3종에서 만든 서버 — 전부 `stop-local.cmd` 또는 검사원의 사후 정리로 종료
- 미끼 python 프로세스 4종, 비-python `TcpListener` 1종 — 전부 종료
- 검사원이 8799에 띄운 probe 서버 — 종료
- 최종 확인: **포트 8765·8799 listener 0개, python 프로세스 0개, `.local-server.pid` 없음**

---

검사원은 제품 소스·테스트·Writing Contract·README를 고치지 않았다. 실패 기준을 낮추거나 테스트를
지우지 않았다. 패키지 설치·lock 파일 변경·인터넷·OpenAI API 호출·`.env` 열기·git commit/push을
하지 않았다. 코드를 고친 뒤 스스로 PASS 처리하지 않았다.
