# 개발 1일차 -> 2일차 검증 보고서 (3차, 독립 검사원)

- 관문: `DEMO-DAY1-GATE`
- run ID: `day1-final-20260822-231600`
- README 버전: 0.23.0-draft
- 시작: 2026-08-22T13:55:00Z / 종료: 2026-08-22T14:18:13Z
- 소스 파일 36개, 통합 해시 `316e103df4213c6a6c4049f140f7933bebfc0dc31853af159f8377bb9058c098`
- 외부 AI 호출 0회, API 비용 0달러, `.env` 열기 0회 (크기 378바이트만 확인)
- 검사원: 구현 담당자와 **다른 새 문맥** (`is_separate_from_implementer=true`)

## 판정: BLOCKED

| 검사 | 내용 | 결과 |
|---|---|---|
| `D1G-01` | 1일차 필수 파일과 실행 진입점 | **PASS** |
| `D1G-02` | 한 주소에서 시작·종료 (문서화된 실행법) | **FAIL** |
| `D1G-03` | 사용자 입력 네 가지 | **PASS** |
| `D1G-04` | Harness와 가짜 AI | **PASS** |
| `D1G-05` | Writing Contract manifest와 설정 | **PASS** |
| `D1G-06` | 반복 가능한 코드 검사 | **PASS** |
| `D1G-07` | 비밀·네트워크·기록 | **PASS** |

## 앞선 두 차단 문제의 현재 상태

| 회차 | 차단 문제 | 현재 | 증거 |
|---|---|---|---|
| 1차 | 경로 탈출로 `.env` 유출 | **해결** | raw socket 68종 + 절대형 요청 2종, 합계 70종 시도해 유출 0건. 가드를 뺀 복제 앱에서는 회귀 시험이 실제로 실패하므로 시험이 형식적이지 않음 |
| 2차 | `start-local.cmd`가 LF 줄바꿈으로 실행 실패 | **해결** | 76줄 전부 CRLF·LF 단독 0개, `.gitattributes`에 `*.cmd text eol=crlf` 고정. 직접 실행 시 2.4초에 정상 종료, 오류 0건, `/api/health` 200, `stop-local.cmd`로 포트 정리 |
| 3차(신규) | 결정형 gate 명령이 비결정적으로 FAIL | **미해결** | `verify-day1.ps1`이 실행기를 pid 기록 전에 강제 종료. 3회 재현에서 1회 실패·2회 성공 |

## 구현 담당자가 주장한 수정 6건 확인 결과

| # | 주장 | 확인 |
|---|---|---|
| 1 | `.cmd` 두 개를 CRLF로 저장하고 `.gitattributes` 고정 | **사실** — start 76/76줄, stop 40/40줄 CRLF, LF 단독 0개 |
| 2 | `timeout`을 `ping`으로 교체 | **사실** — `ping -n 2 127.0.0.1 >nul` 사용, 입력 리디렉션 환경에서 정상 동작 |
| 3 | 경로 탈출 시험을 인코딩 7종 + raw socket 7종으로 분리 | **사실이고 실질적** — 변이 시험으로 유효성 확인. 다만 `frontend/dist`가 없으면 14종이 공허하게 통과하는 구멍이 남음 |
| 4 | `GET /api/runs/{id}`에 localhost Host 검사 적용 | **사실** — `Host: evil.example.com`으로 403 `NON_LOCAL_HOST` |
| 5 | `verify-day1.ps1`이 `start/stop-local.cmd`를 실제로 실행 | **부분적** — 실행은 하지만 준비되자마자 강제 종료해 실행기 뒷부분을 검사하지 못하고, 결과가 흔들리며 서버를 고아로 남긴다 |
| 6 | 미룬 일을 `day-gate-verifier.md`에 명시 | **사실** — 멱등 기록 2시간 보관(누적 14일차), 브라우저 열림 사람 확인 2건이 표로 적혀 있음 |

### D1G-01 1일차 필수 파일과 실행 진입점 — PASS

- script D1G-01: 필수 파일 28개 모두 존재, 누락 0건
- backend 모듈 로드 exit 0 (app.main, app.api.runs, app.harness.orchestrator)
- 검사원이 backend/app 16개 파일, frontend/src 8개 파일, tests 2개 파일을 직접 읽어 React·FastAPI·Harness 뼈대·검증 도구가 모두 실재함을 확인
- pytest collect-only가 67개 시험을 수집 (수집 오류 0건)

실행한 명령:

- `powershell -File scripts\verify-day1.ps1 (D1G-01 구간)` -> exit 0 — 필수 파일 28개 모두 존재
- `python -c "import app.main, app.api.runs, app.harness.orchestrator; print('modules ok')"` -> exit 0 — modules ok
- `python -m pytest ../tests -q --collect-only` -> exit 0 — 67 tests collected in 0.48s

### D1G-02 한 주소에서 시작·종료 (문서화된 실행법) — FAIL

- 줄바꿈은 고쳐졌다: start-local.cmd 76줄 전부 CRLF·LF 단독 0개, stop-local.cmd 40줄 전부 CRLF·LF 단독 0개. .gitattributes에 `*.cmd text eol=crlf` 고정 확인
- 사람이 쓰는 방식대로 직접 실행하면 정상이다: start-local.cmd가 2.4초에 정상 종료하고 오류 메시지 0건, /api/health 200, stop-local.cmd로 8765 포트가 0개로 정리됨
- 그러나 계약이 지정한 결정형 gate 명령이 FAIL로 끝났다: 'stop-local.cmd 뒤에도 8765 포트가 열려 있습니다' (보고서 day1-20260822-230550.json)
- 원인은 scripts/verify-day1.ps1의 shortcut job이다. /api/health가 200이 되는 즉시 `Stop-Process -Id $proc.Id -Force`로 실행기를 강제 종료하는데, start-local.cmd는 그 뒤 약 0.4~1초 동안 `.local-server.pid`를 아직 쓰지 않은 상태다
- 동일 논리를 3회 재현한 결과 1회 실패·2회 성공으로 gate 자체가 비결정적이다. 같은 소스에서 PASS와 BLOCKED가 번갈아 나오므로 관문 역할을 할 수 없다
- 제품 쪽 결함도 함께 드러났다: 실행기가 uvicorn을 띄운 뒤 pid 기록 전에 끊기면 서버가 고아가 된다. 이때 stop-local.cmd는 '이 스크립트로 시작한 서버가 없습니다'라며 exit 0으로 끝나고, start-local.cmd는 포트 사용 중이라며 stop-local.cmd를 먼저 실행하라고 안내한다. 비전공자에게 빠져나갈 길이 없는 막다른 상태다
- script 실행 뒤 8765에 python PID 37052가 남아 있어 검사원이 직접 종료했다 (자신이 시작한 프로세스만 정상 종료 기준 위반)

실행한 명령:

- `powershell -ExecutionPolicy Bypass -File scripts\verify-day1.ps1` -> exit 1 — 판정 BLOCKED — D1G-02 FAIL: stop-local.cmd 뒤에도 8765 포트가 열려 있습니다.
- `cmd /c start-local.cmd (검사원이 직접, 중간에 끊지 않음)` -> exit 0 — 2.4초 만에 정상 종료, 오류 출력 0건, .local-server.pid=33824(7바이트), 8765 LISTENING
- `curl/Invoke-WebRequest http://127.0.0.1:8765/api/health` -> exit 0 — HTTP 200
- `cmd /c stop-local.cmd (검사원이 직접)` -> exit 0 — '서버를 종료했습니다.' 출력, pid 파일 삭제, 8765 listener 0개
- `verify-day1.ps1의 shortcut job 논리를 3회 재현` -> exit 1 — run#1 pidfile 미작성·포트 잔존(FAIL), run#2·run#3 정상(PASS) — 경합으로 결과가 흔들림

### D1G-03 사용자 입력 네 가지 — PASS

- 화면(NewRunScreen.tsx)에 '보도 목적'·'공식 자료'·'공개 범위'·'자료 확인 기준일' 라벨이 모두 있고 각각 required 표시
- 검사원이 직접 부른 API 결과 — 보도 목적 빈값 -> issues=['PURPOSE'], 공식 자료 없음 -> ['OFFICIAL_SOURCE'], 공개 범위 EMBARGO -> ['DISCLOSURE'], 기준일 미래 -> ['BASIS_DATE'], 전송 미확인 -> ['EXTERNAL_AI_TRANSFER_CONFIRMED']
- 다섯 경우 모두 state=NEEDS_INPUT, status_label='입력 보완 필요', draft_version=0 — 거짓 성공 0건
- 다섯 경우 모두 actual_model_calls=0, estimated_cost_usd=0 — 입력 Gate에서 막히므로 외부 호출 이전에 차단
- 요청 토큰 쿠키 없이 보낸 POST /api/runs는 403 REQUEST_TOKEN_INVALID로 거부
- 형식 위반 입력은 422와 pydantic 오류 목록으로 끝나며 내부 경로·비밀값이 섞이지 않음

실행한 명령:

- `grep 'NewRunScreen.tsx' 라벨 확인` -> exit 0 — 보도 목적/공식 자료/공개 범위/자료 확인 기준일 라벨 4종 모두 존재
- `POST /api/runs 잘못된 입력 5종 -> GET /api/runs/{id} (검사원 직접 호출)` -> exit 0 — 5종 모두 NEEDS_INPUT, draft_version=0, actual_model_calls=0, estimated_cost_usd=0
- `POST /api/runs (요청 쿠키 없이)` -> exit 0 — 403 REQUEST_TOKEN_INVALID

### D1G-04 Harness와 가짜 AI — PASS

- 새 Run ID 형식이 계약과 일치: RUN-A3E7070F7D00 (^RUN-[A-Z0-9]{12}$), 생성 응답 202, 초기 상태 CREATED
- 1일차 최소 전이 확인: CREATED -> VALIDATING_INPUT -> (NEEDS_INPUT | EXTRACTING_FACTS). 정상 입력은 아직 없는 단계를 성공으로 위장하지 않고 FAILED/TECHNICAL/DAY1_SCOPE_LIMIT로 정직하게 멈춤
- 가짜 ModelGateway만 사용: /api/health가 model_gateway=fake, external_api_calls=0을 보고. 검증 전 구간에서 실제 OpenAI 호출 0회, 비용 0달러
- 멱등: 같은 client_request_id + 같은 내용 -> 같은 run_id 재사용, 같은 키 + 다른 내용 -> 409 IDEMPOTENCY_KEY_REUSED
- 삭제 뒤 조회는 404 RUN_NOT_FOUND
- 상태 머신 시험 포함 pytest 67개 전부 통과 (전이표가 RunState 전체를 덮고, 종료 상태 DRAFT_READY·FAILED에서 나가는 전이 0개)

실행한 명령:

- `POST /api/runs (정상 입력) -> 폴링 (검사원 직접 호출)` -> exit 0 — 202 / run_id=RUN-A3E7070F7D00 / CREATED -> FAILED(DAY1_SCOPE_LIMIT)
- `POST /api/runs 같은 멱등 키 2회 / 같은 키+다른 내용` -> exit 0 — 같은 run_id 재사용 / 409 IDEMPOTENCY_KEY_REUSED
- `GET /api/health` -> exit 0 — model_gateway=fake, external_api_calls=0
- `python -m pytest ../tests -q` -> exit 0 — 67 passed, 3 warnings in 3.11s

### D1G-05 Writing Contract manifest와 설정 — PASS

- 물리적 파일 5개 확인: contract.yaml, profile.yaml, template.yaml, style.yaml, validation.yaml
- 서버가 실제로 읽음: load_writing_contract()가 assembly_member_partial_amendment_plenary_v1@1.0.0을 반환
- ID·버전 검사가 실질적으로 동작: manifest의 components 선언과 각 파일의 id@version이 다르면 ContractLoadError로 서버가 시작하지 않음 (시험으로 확인)
- 필수 필드 검사 존재: profile/template/style/validation 각각 4개 필수 필드를 확인
- /api/health의 writing_contract_files가 5개, /api/contract의 components 4개가 모두 @1.0.0
- contract_loader가 절대 경로가 아니라 파일 이름만 노출하므로 응답에 내부 경로가 새지 않음

실행한 명령:

- `powershell -File scripts\verify-day1.ps1 (D1G-05 구간)` -> exit 0 — 물리적 파일 5개 존재
- `python -c "from app.harness.contract_loader import load_writing_contract as l; c=l(); print(c.full_id)"` -> exit 0 — assembly_member_partial_amendment_plenary_v1@1.0.0
- `GET /api/health, GET /api/contract` -> exit 0 — writing_contract_files 5개, components 4개 모두 @1.0.0

### D1G-06 반복 가능한 코드 검사 — PASS

- 백엔드 pytest 67개 통과, exit 0 (검사원이 script와 별개로 다시 실행)
- 프런트 typecheck exit 0 (tsc --noEmit)
- 프런트 vitest 6개 통과, exit 0
- 프런트 production build exit 0, dist/index.html·assets 2개 생성
- 로컬 API/UI smoke: /api/health 200, / 200에 <div id="root"> 포함
- 필수 명령 누락·SKIP 0건
- 다만 이 네 명령을 감싸는 전체 gate script는 exit 1이며 D1G-02 구간이 재현되지 않는다 (D1G-02의 차단 문제 참조)

실행한 명령:

- `python -m pytest ../tests -q` -> exit 0 — 67 passed in 3.11s
- `npx tsc --noEmit -p tsconfig.app.json` -> exit 0 — 출력 없음
- `npm test --silent` -> exit 0 — Test Files 1 passed, Tests 6 passed
- `npm run build --silent` -> exit 0 — 32 modules transformed, built in 464ms

### D1G-07 비밀·네트워크·기록 — PASS

- 경로 탈출 차단 확인 (1차 차단 문제): 검사원이 HTTP 클라이언트를 거치지 않고 요청 줄을 직접 쓴 raw socket 68종 + 절대형(absolute-form) 요청 2종, 합계 70종을 시도해 유출 0건. `..` 원시형, %2e/%2f/%5c 인코딩, 이중 인코딩, overlong UTF-8, NULL 바이트, NTFS 대체 데이터 스트림(::$DATA), 끝의 점·공백, 8.3 단축 이름, 드라이브 절대경로, UNC, 예약 장치 이름(CON/NUL/COM1/aux), /assets mount 경유, 질의문자열·프래그먼트 혼합을 포함
- 요청이 실제로 정규화되지 않은 채 서버에 도달함을 증명: `/api/../../../.env`가 spa()의 api 분기 404 JSON(133바이트)을 돌려주고 `/assets/../../../.env`가 StaticFiles의 404(22바이트)를 돌려주므로, 중간 계층이 `..`를 미리 정리하지 않았다
- 회귀 시험이 형식적이지 않음: 제품 소스를 고치지 않고, main.py의 spa() 경로 논리를 그대로 복제하되 `is_relative_to` 가드만 뺀 앱과 진짜 .env가 아닌 미끼 파일로 변이 시험을 했다. tests/test_harness_flow.py의 _assert_no_leak을 그대로 불러 적용한 결과 가드가 있으면 통과, 가드를 빼면 AssertionError로 실패했다
- Host 검사가 조회에도 적용됨 (구현 담당자 주장 4 확인): raw socket으로 `Host: evil.example.com`을 보낸 GET /api/runs/{id}가 403 NON_LOCAL_HOST
- 오류 메시지 누출 0건: 잘못된 JSON·schema 위반·없는 주소·허용되지 않은 method·4000자 run_id에 대해 내부 경로(C:\, policy_agent, site-packages), traceback, 비밀값이 전혀 나오지 않음. /docs·/redoc·/openapi.json은 비활성이라 첫 화면으로 되돌아감
- 브라우저 bundle 청정: dist의 js·css·html에 API 키 패턴 0건, 절대 경로 0건
- 실제 OpenAI 호출 경로 0건, 비용 0달러, wildcard CORS 0건, 생명과학 전용 잔재 0건, dangerouslySetInnerHTML 0건, 코드의 .env 읽기 0건
- 검사원은 .env를 열지 않았고 크기(378바이트)만 확인했다

실행한 명령:

- `raw socket 경로 탈출 68종 + absolute-form 요청줄 2종 (검사원 자체 probe)` -> exit 0 — 70종 전부 차단, 유출 0건
- `raw socket: GET /api/runs/{id} with Host: evil.example.com` -> exit 0 — 403 NON_LOCAL_HOST
- `변이 시험: 가드를 뺀 복제 앱에 tests의 _assert_no_leak 적용` -> exit 0 — 가드 있음 -> 통과, 가드 없음 -> AssertionError. 회귀 시험이 실제로 유출을 잡음
- `오류 응답 9종 본문 검사 (422/404/405/긴 run_id/docs·redoc·openapi)` -> exit 0 — 내부 경로·traceback·비밀값 0건
- `grep 비밀 패턴 frontend/dist/assets/*.js frontend/dist/index.html` -> exit 0 — API 키 패턴 0건, 절대 경로 0건

## 차단 문제

- `D1G-02`: 계약이 지정한 결정형 gate 명령(scripts/verify-day1.ps1)이 exit 1·BLOCKED로 끝났다. scripts/verify-day1.ps1의 shortcut job이 /api/health 200을 확인한 즉시 start-local.cmd를 Stop-Process -Force로 끊는데, 그 시점에 실행기는 아직 .local-server.pid를 쓰지 않았다. 그래서 stop-local.cmd가 서버를 찾지 못하고 8765 포트가 남는다. 같은 논리를 3회 재현했더니 1회 실패·2회 성공이라 gate가 비결정적이다.
- `D1G-02`: 실행기가 uvicorn을 띄운 뒤 pid 기록 전에 끊기면 서버가 고아가 되고, stop-local.cmd는 '이 스크립트로 시작한 서버가 없습니다'로 exit 0, start-local.cmd는 포트 사용 중이라며 stop-local.cmd를 먼저 실행하라고 안내한다. 비전공자가 빠져나갈 방법이 없다. 이번 검증에서도 8765에 남은 python PID 37052를 검사원이 직접 종료해야 했다.

## 경고 (차단은 아니지만 남겨 둔다)

- /api/health·/api/bootstrap·/api/contract에는 localhost Host 검사가 없다. 다른 이름으로 이 서버를 가리키는 페이지가 앱 제목·Contract ID·모델 이름·입력 한도를 읽고 요청 토큰 쿠키까지 받아 갈 수 있다. 상태 변경과 GET /api/runs/{id}는 막혀 있어 사용자 자료는 새지 않으므로 차단이 아닌 경고로 남긴다.
- tests/test_harness_flow.py의 경로 탈출 시험 14종은 frontend/dist가 없으면 공허하게 통과한다. dist가 없으면 SPA 경로가 등록되지 않아 모두 404가 되고 _assert_no_leak이 `status != 200`에서 바로 돌아온다. 빌드 산출물 존재를 시험 안에서 확인하지 않는다.
- start-local.cmd의 실패 분기에 `pause`가 있어, 입력이 없는 환경(CI·자동 검증)에서 실행하면 그 분기에 들어갈 때 프로세스가 멈춘다.
- start-local.cmd가 만드는 .local-server.pid가 .gitignore에 없다. 서버가 떠 있는 동안 git status에 추적되지 않은 파일로 나타난다.
- GET /api/runs/{긴 문자열}이 입력받은 run_id를 그대로 응답에 되비춘다(4000자 확인). JSON이고 화면은 React가 escape하므로 현재 위험은 없으나 반사 자체는 줄이는 편이 낫다.
- 응답에 server: uvicorn 헤더가 그대로 나가고, 첫 화면 응답에 CSP·X-Content-Type-Options 같은 보안 헤더가 없다. README 계획상 누적 10일차(안전 보강 4일차) 범위다.
- 미룬 일은 verification/day-gate-verifier.md에 숨기지 않고 적혀 있다 (멱등 기록 2시간 보관 -> 누적 14일차, 실행 바로가기의 브라우저 열림 사람 확인 -> 1일차 종료 확인).

## 1일차 종료 조건 (README §3.7 누적 2일차)

| 종료 조건 | 결과 |
|---|---|
| 실행 바로가기에서 브라우저가 열림 | 조건부 충족 — `start-local.cmd`가 `start "" http://127.0.0.1:8765`까지 정상 도달함을 확인했다. 창이 실제로 보였는지는 사람이 눈으로 봐야 하며, `day-gate-verifier.md`도 이를 미룬 일로 적어 두었다 |
| 보도 목적·공식 자료·공개 범위·자료 기준일 입력 가능 | 충족 — 화면 라벨 4종과 API 계약 모두 확인, 잘못된 값 5종이 거짓 성공 없이 차단 |
| Writing Contract manifest 1개 + 설정 4개를 서버가 읽음 | 충족 — 물리 파일 5개, `assembly_member_partial_amendment_plenary_v1@1.0.0` 로드 |
| 다른 새 문맥의 DayGateVerifier가 D1G-01~07을 확인해 최신 `DEMO-DAY1-GATE=PASS` 보고서를 남김 | **미충족** — 이 보고서의 판정은 `BLOCKED`다 |

따라서 **개발 2일차(누적 영업일 3) 범위는 시작할 수 없다.** README §3.7의 누적 3일차 항목은
"현재 소스 snapshot과 일치하는 최신 `DEMO-DAY1-GATE=PASS`가 있을 때만 시작"으로 못박고 있다.

## 검사원이 정리한 것

- `verify-day1.ps1`이 남긴 8765의 python PID 37052 종료
- 재현 시험에서 생긴 고아 서버 종료
- 검사원이 8799에 띄운 probe 서버 종료
- 최종 확인: 포트 8765·8799 listener 0개, python 프로세스 0개

---

검사원은 제품 소스·테스트·Writing Contract·README를 고치지 않았다. 코드를 고친 뒤
스스로 PASS 처리하지 않았다. 소스 snapshot 해시가 script 실행 시점과 같음을 확인했으므로
이 보고서는 현재 트리에 대해 유효하다.