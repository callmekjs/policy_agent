{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "title": "AuditDraftAgent 응답",
  "description": "칸별로 쓴 본문. slot의 실제 목록은 코드(SLOT_ORDER)에서 끼워 넣는다 — 여기에 손으로 베껴 두면 코드와 어긋난다.",
  "type": "object",
  "properties": {
    "slots": {
      "type": "array",
      "description": "채운 칸만 담는다. 못 채우는 칸에 쓴 글은 어차피 버려진다.",
      "items": {
        "type": "object",
        "properties": {
          "slot": {
            "type": "string",
            "description": "칸 이름. 정해진 목록 밖의 값은 받지 않는다."
          },
          "text": {
            "type": "string",
            "description": "그 칸의 본문. 원장에 없는 숫자는 쓸 수 없다."
          },
          "fact_ids": {
            "type": "array",
            "description": "이 문장이 기대는 사실의 번호. 없으면 빈 목록.",
            "items": { "type": "string" }
          }
        },
        "required": ["slot", "text", "fact_ids"],
        "additionalProperties": false
      }
    }
  },
  "required": ["slots"],
  "additionalProperties": false
}
